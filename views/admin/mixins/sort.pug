mixin sort()
    div(sort)
        select(class="form-control" name="sort" sort-select)
            option(disabled) --Chọn hành động--
            option(value="position-desc" ) Vị trí giảm dần
            option(value="position-asc" ) Vị trí tăng dần
            option(value="price-desc" ) Giá giảm dần
            option(value="position-asc") Giá tăng dần
            option(value="title-asc") Tiêu đề từ A-Z
            option(value="title-desc") Tiêu đề từ Z-A
        button(class="btn btn-danger mt-2" sort-clear) Clear
mixin sortOrder()
    div(sort)
        select(class="form-control" name="sort" sort-select)
            option(disabled) --Chọn hành động--
            option(value="createdAt-desc" ) Thời gian đặt giảm dần
            option(value="createdAt-asc" ) Thời gian đặt tăng dần
            
        button(class="btn btn-danger mt-2" sort-clear) Clear
mixin sortCategory()
    div(sort)
        select(class="form-control" name="sort" sort-select)
            option(disabled) --Chọn hành động--
            option(value="position-desc" ) Vị trí giảm dần
            option(value="position-asc" ) Vị trí tăng dần
            option(value="title-asc") Tiêu đề từ A-Z
            option(value="title-desc") Tiêu đề từ Z-A
        button(class="btn btn-danger mt-2" sort-clear) Clear


